# Port targets — `tungstenite-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `tungstenite` that codex-rs uses, and therefore the minimum subset that `tungstenite-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-174] (rank `94`, tier `direct`, status `To Do`)

**Cargo package name**: `tungstenite`
**Rust module name (in `use` statements)**: `tungstenite`

## Summary

- **7** distinct symbols imported by codex-rs
- **8** total `use` statements
- **3** codex source files affected
- **2** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `tungstenite::protocol::WebSocketConfig`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-api`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `codex-api/src/endpoint/realtime_websocket/methods.rs:45` (`codex-api`) — `use tungstenite::protocol::WebSocketConfig`
- `codex-api/src/endpoint/responses_websocket.rs:46` (`codex-api`) — `use tungstenite::protocol::WebSocketConfig`

### `tungstenite::Message`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-app-server-test-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server-test-client/src/lib.rs:82` (`codex-app-server-test-client`) — `use tungstenite::Message`

### `tungstenite::WebSocket`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-app-server-test-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server-test-client/src/lib.rs:83` (`codex-app-server-test-client`) — `use tungstenite::WebSocket`

### `tungstenite::connect`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-app-server-test-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server-test-client/src/lib.rs:84` (`codex-app-server-test-client`) — `use tungstenite::connect`

### `tungstenite::stream::MaybeTlsStream`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-app-server-test-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server-test-client/src/lib.rs:85` (`codex-app-server-test-client`) — `use tungstenite::stream::MaybeTlsStream`

### `tungstenite::extensions::ExtensionsConfig`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-api`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `codex-api/src/endpoint/responses_websocket.rs:44` (`codex-api`) — `use tungstenite::extensions::ExtensionsConfig`

### `tungstenite::extensions::compression::deflate::DeflateConfig`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-api`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `codex-api/src/endpoint/responses_websocket.rs:45` (`codex-api`) — `use tungstenite::extensions::compression::deflate::DeflateConfig`
